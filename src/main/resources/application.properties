# ===============================
# DATABASE CONFIGURATION (POSTGRESQL for Render)
# ===============================
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://dpg-d4mrmpvgi27c738hibrg-a:5432/Drealestate_db_ofvy}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:realestate_db_ofvy_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:dsyR4nDA7l6EJgXwlxXGjzJRSiiHO8TW}
spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================
# JPA / HIBERNATE CONFIGURATION
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# ===============================
# VIEW (JSP) CONFIGURATION
# ===============================
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp

# ===============================
# SERVER CONFIGURATION (REQUIRED FOR RENDER)
# ===============================
server.port=${PORT:8080}
